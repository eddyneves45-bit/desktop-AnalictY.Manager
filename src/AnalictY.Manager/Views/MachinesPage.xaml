<UserControl x:Class="AnalictY.Manager.Views.MachinesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="760" d:DesignWidth="1280"
             Background="#F3F5F7"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <Style x:Key="WhiteCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="20"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ReadableTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">
                <!-- Header -->
                <Border Style="{StaticResource WhiteCard}" Margin="0,0,0,24">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Button Width="40" Height="40" Margin="0,0,16,0" Background="Transparent" BorderThickness="0" Click="NavigateBackButton_Click">
                            <Path Data="M10 19l-7-7m0 0l7-7m-7 7h18" Stroke="#6B7280" StrokeThickness="2" Fill="Transparent" Width="20" Height="20" Stretch="Uniform"/>
                        </Button>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Gestão de Máquinas" Foreground="#111827" FontSize="24" FontWeight="Bold"/>
                            <TextBlock Text="Organize máquinas por pastas e subpastas" Foreground="#6B7280" FontSize="14" Margin="0,4,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <Button Content="Nova Pasta" Style="{StaticResource SecondaryButton}" Command="{Binding CreateFolderCommand}" Margin="0,0,12,0"/>
                            <Button Content="Adicionar Máquina" Style="{StaticResource PrimaryButton}" Command="{Binding CreateMachineCommand}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border x:Name="ErrorBorder" Background="#FEF2F2" BorderBrush="#FCA5A5" BorderThickness="1" CornerRadius="8"
                        Padding="14" Margin="0,0,0,14" Visibility="Collapsed">
                    <TextBlock x:Name="ErrorText" Foreground="#DC2626" FontWeight="SemiBold" TextWrapping="Wrap"/>
                </Border>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Sidebar: Structure -->
                    <Border Grid.Column="0" Style="{StaticResource WhiteCard}" Margin="0,0,16,0" VerticalAlignment="Top">
                        <StackPanel>
                            <TextBlock Text="ESTRUTURA" Foreground="#6B7280" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,12"/>
                            
                            <Button Content="Todas" Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Command="{Binding SelectFolderCommand}" BorderThickness="0" Background="Transparent" Margin="0,0,0,4">
                                <Button.CommandParameter>
                                    <x:Null/>
                                </Button.CommandParameter>
                            </Button>
                            
                            <TreeView ItemsSource="{Binding RootFolders}" BorderThickness="0" Background="Transparent">
                                <TreeView.ItemContainerStyle>
                                    <Style TargetType="TreeViewItem">
                                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                                        <EventSetter Event="Selected" Handler="OnFolderSelected"/>
                                    </Style>
                                </TreeView.ItemContainerStyle>
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding SubFolders}">
                                        <StackPanel Orientation="Horizontal" Margin="2">
                                            <Path Data="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z" Stroke="#F59E0B" Fill="#FEF3C7" StrokeThickness="1.5" Width="16" Height="16" Stretch="Uniform" Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding Name}" Foreground="#374151" FontSize="14"/>
                                        </StackPanel>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </StackPanel>
                    </Border>

                    <!-- Main Content: Breadcrumbs, Search and List -->
                    <StackPanel Grid.Column="1">
                        <!-- Breadcrumbs and Search -->
                        <Border Style="{StaticResource WhiteCard}" Margin="0,0,0,16" Padding="16">
                            <StackPanel>
                                <ItemsControl ItemsSource="{Binding Breadcrumbs}" Margin="0,0,0,12">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="/" Foreground="#9CA3AF" Margin="8,0" VerticalAlignment="Center"/>
                                                <Button Content="{Binding Name}" Command="{Binding DataContext.SelectFolderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Foreground="#6B7280" Background="Transparent" BorderThickness="0" Padding="4,2"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                
                                <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="12" Padding="12,0">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Path Data="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" Stroke="#9CA3AF" StrokeThickness="2" Width="20" Height="20" Stretch="Uniform" Margin="0,0,12,0"/>
                                        <TextBox Grid.Column="1" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Padding="0,12" VerticalContentAlignment="Center" FontSize="15" Tag="Buscar nesta pasta..."/>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- Machines List -->
                        <Border Style="{StaticResource WhiteCard}" Padding="0">
                            <StackPanel>
                                <ItemsControl ItemsSource="{Binding FilteredMachines}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#F3F4F6" BorderThickness="0,0,0,1" Padding="24">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    
                                                    <StackPanel>
                                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                                            <TextBlock Text="{Binding Name}" Foreground="#111827" FontSize="18" FontWeight="Bold"/>
                                                            <Border Background="{Binding StatusBackground}" CornerRadius="12" Padding="8,2" Margin="12,0,0,0">
                                                                <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusColor}" FontSize="12" FontWeight="SemiBold"/>
                                                            </Border>
                                                        </StackPanel>
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="Código: " Foreground="#6B7280" FontSize="14"/>
                                                            <TextBlock Text="{Binding Code}" Foreground="#374151" FontSize="14" FontWeight="Medium" Margin="0,0,24,0"/>
                                                            <TextBlock Text="Centro de Custo: " Foreground="#6B7280" FontSize="14"/>
                                                            <TextBlock Text="{Binding CostCenter}" Foreground="#374151" FontSize="14" FontWeight="Medium" Margin="0,0,24,0"/>
                                                            <TextBlock Text="Setor: " Foreground="#6B7280" FontSize="14" Visibility="{Binding Location, Converter={StaticResource StringToVisibilityConverter}}"/>
                                                            <TextBlock Text="{Binding Location}" Foreground="#374151" FontSize="14" FontWeight="Medium" Visibility="{Binding Location, Converter={StaticResource StringToVisibilityConverter}}"/>
                                                        </StackPanel>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                                        <Button Command="{Binding DataContext.EditMachineCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0">
                                                            <Path Data="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" Stroke="#374151" StrokeThickness="2" Width="18" Height="18" Stretch="Uniform"/>
                                                        </Button>
                                                        <Button Command="{Binding DataContext.DeleteMachineCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource SecondaryButton}">
                                                            <Path Data="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" Stroke="#DC2626" StrokeThickness="2" Width="18" Height="18" Stretch="Uniform"/>
                                                        </Button>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <!-- Machine Modal -->
        <Grid Visibility="{Binding ShowMachineModal, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border Background="#66000000"/>
            <Border Style="{StaticResource WhiteCard}" Width="450" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="0">
                <StackPanel>
                    <Border BorderBrush="#F3F4F6" BorderThickness="0,0,0,1" Padding="24,20">
                        <StackPanel>
                            <TextBlock Text="{Binding IsEditingMachine, Converter={StaticResource FormTitleConverter}, ConverterParameter='Máquina'}" Foreground="#111827" FontSize="20" FontWeight="Bold"/>
                            <TextBlock Text="Cadastre ou edite os dados da máquina" Foreground="#6B7280" FontSize="14" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Padding="24">
                        <StackPanel>
                            <TextBlock Text="Nome *" Style="{StaticResource LabelText}"/>
                            <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding MachineName, Mode=TwoWay}" Margin="0,0,0,16"/>
                            
                            <TextBlock Text="Código *" Style="{StaticResource LabelText}"/>
                            <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding MachineCode, Mode=TwoWay}" Margin="0,0,0,16"/>
                            
                            <TextBlock Text="Centro de Custo *" Style="{StaticResource LabelText}"/>
                            <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding CostCenter, Mode=TwoWay}" Margin="0,0,0,16"/>
                            
                            <TextBlock Text="Setor" Style="{StaticResource LabelText}"/>
                            <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding Location, Mode=TwoWay}" Margin="0,0,0,16"/>
                            
                            <CheckBox Content="Máquina Ativa" IsChecked="{Binding IsActive, Mode=TwoWay}" FontWeight="SemiBold" Foreground="#374151" Margin="0,8"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Background="#F9FAFB" Padding="24,16" CornerRadius="0,0,16,16">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Cancelar" Style="{StaticResource SecondaryButton}" Command="{Binding CloseModalsCommand}" Margin="0,0,12,0" Width="100"/>
                            <Button Content="Salvar" Style="{StaticResource PrimaryButton}" Command="{Binding SaveMachineCommand}" Width="100"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Folder Modal -->
        <Grid Visibility="{Binding ShowFolderModal, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border Background="#66000000"/>
            <Border Style="{StaticResource WhiteCard}" Width="450" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="0">
                <StackPanel>
                    <Border BorderBrush="#F3F4F6" BorderThickness="0,0,0,1" Padding="24,20">
                        <StackPanel>
                            <TextBlock Text="{Binding IsEditingFolder, Converter={StaticResource FormTitleConverter}, ConverterParameter='Pasta'}" Foreground="#111827" FontSize="20" FontWeight="Bold"/>
                            <TextBlock Text="Defina o nome e tipo da pasta" Foreground="#6B7280" FontSize="14" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Padding="24">
                        <StackPanel>
                            <TextBlock Text="Nome da Pasta *" Style="{StaticResource LabelText}"/>
                            <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding FolderName, Mode=TwoWay}" Margin="0,0,0,16"/>
                            
                            <CheckBox Content="É um Setor" IsChecked="{Binding IsSector, Mode=TwoWay}" FontWeight="SemiBold" Foreground="#374151" Margin="0,8"/>
                            
                            <Button Content="Excluir Pasta" Style="{StaticResource SecondaryButton}" Command="{Binding DeleteFolderCommand}" Foreground="#DC2626" BorderBrush="#FCA5A5" Visibility="{Binding IsEditingFolder, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,16,0,0"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Background="#F9FAFB" Padding="24,16" CornerRadius="0,0,16,16">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Cancelar" Style="{StaticResource SecondaryButton}" Command="{Binding CloseModalsCommand}" Margin="0,0,12,0" Width="100"/>
                            <Button Content="Salvar" Style="{StaticResource PrimaryButton}" Command="{Binding SaveFolderCommand}" Width="100"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>

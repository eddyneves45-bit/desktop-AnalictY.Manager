<UserControl x:Class="AnalictY.Manager.Views.TagsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="ManagerPanelBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#30465F" Offset="0" />
            <GradientStop Color="#243447" Offset="1" />
        </LinearGradientBrush>

        <Style x:Key="ManagerCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource ManagerPanelBrush}" />
            <Setter Property="BorderBrush" Value="#4A617B" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="0,0,12,12" />
        </Style>

        <Style x:Key="ManagerTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="ManagerMutedStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AAB7C5" />
            <Setter Property="FontSize" Value="12" />
        </Style>

        <Style x:Key="MetricValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="AdminDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#E5EDF5" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#4A617B" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="RowHeaderWidth" Value="0" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="IsReadOnly" Value="True" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2A3D53" />
            <Setter Property="Foreground" Value="#D8E2EC" />
            <Setter Property="BorderBrush" Value="#4A617B" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#E5EDF5" />
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,6" />
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <Border Style="{StaticResource ManagerCardStyle}" Margin="0,0,0,12" Padding="22,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Gerenciamento de TAGs" Style="{StaticResource ManagerTitleStyle}" />
                    <TextBlock Text="Monitoramento e status das TAGs do AnalictY Server" Style="{StaticResource ManagerMutedStyle}" Margin="0,6,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <ComboBox ItemsSource="{Binding Machines}" SelectedItem="{Binding SelectedMachine}" Width="140" Margin="0,0,8,0" />
                    <ComboBox ItemsSource="{Binding Drivers}" SelectedItem="{Binding SelectedDriver}" Width="140" Margin="0,0,8,0" />
                    <TextBox Text="{Binding SearchText}" Width="200" Margin="0,0,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource ManagerCardStyle}">
                <StackPanel>
                    <TextBlock Text="Total" Style="{StaticResource ManagerMutedStyle}" />
                    <TextBlock Text="{Binding TotalTags}" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource ManagerCardStyle}">
                <StackPanel>
                    <TextBlock Text="Ativas" Style="{StaticResource ManagerMutedStyle}" />
                    <TextBlock Text="{Binding ActiveTags}" Foreground="#72D44E" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource ManagerCardStyle}">
                <StackPanel>
                    <TextBlock Text="Sem atualização" Style="{StaticResource ManagerMutedStyle}" />
                    <TextBlock Text="{Binding StaleTags}" Foreground="#FACC15" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource ManagerCardStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Com erro" Style="{StaticResource ManagerMutedStyle}" />
                    <TextBlock Text="{Binding ErrorTags}" Foreground="#EF4444" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Style="{StaticResource ManagerCardStyle}" Margin="0">
            <StackPanel>
                <TextBlock Text="TAGs - Lista Completa" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,10" />
                <DataGrid ItemsSource="{Binding TagRows}" Style="{StaticResource AdminDataGridStyle}" MinHeight="300">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nome da TAG" Binding="{Binding Name}" Width="1.5*" />
                        <DataGridTextColumn Header="Máquina" Binding="{Binding Machine}" Width="*" />
                        <DataGridTextColumn Header="Driver" Binding="{Binding Driver}" Width="0.8*" />
                        <DataGridTextColumn Header="Endereço" Binding="{Binding Address}" Width="1.2*" />
                        <DataGridTextColumn Header="Valor" Binding="{Binding Value}" Width="0.7*" />
                        <DataGridTextColumn Header="Qualidade" Binding="{Binding Quality}" Width="0.8*" />
                        <DataGridTextColumn Header="Atualização" Binding="{Binding UpdatedAt}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>

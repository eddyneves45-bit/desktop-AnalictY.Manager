<UserControl x:Class="AnalictY.Manager.Views.AdminDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="ManagerPanelBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#30465F" Offset="0" />
            <GradientStop Color="#243447" Offset="1" />
        </LinearGradientBrush>

        <Style x:Key="ManagerCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource ManagerPanelBrush}" />
            <Setter Property="BorderBrush" Value="#4A617B" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="0,0,12,12" />
        </Style>

        <Style x:Key="ManagerTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="ManagerMutedStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AAB7C5" />
            <Setter Property="FontSize" Value="12" />
        </Style>

        <Style x:Key="MetricValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="AdminDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#E5EDF5" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#4A617B" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="RowHeaderWidth" Value="0" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="IsReadOnly" Value="True" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#DC2626" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#DC2626" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,9" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B91C1C" />
                    <Setter Property="BorderBrush" Value="#B91C1C" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#42617F" />
            <Setter Property="Foreground" Value="#E5EDF5" />
            <Setter Property="BorderBrush" Value="#334456" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,9" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A617B" />
                    <Setter Property="BorderBrush" Value="#42556A" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2A3D53" />
            <Setter Property="Foreground" Value="#D8E2EC" />
            <Setter Property="BorderBrush" Value="#4A617B" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#E5EDF5" />
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,6" />
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <Border Style="{StaticResource ManagerCardStyle}" Margin="0,0,0,12" Padding="22,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="170" />
                    <ColumnDefinition Width="170" />
                    <ColumnDefinition Width="210" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Ellipse Width="44" Height="44" Fill="#72D44E" VerticalAlignment="Center" />

                <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding ServerStatus}" Foreground="#72D44E" FontSize="20" FontWeight="Bold" />
                    <TextBlock Text="{Binding UptimeText}" Foreground="#C7D2DE" FontSize="12" Margin="0,4,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <TextBlock Text="Versão" Style="{StaticResource ManagerMutedStyle}" />
                    <TextBlock Text="{Binding Version}" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="3" VerticalAlignment="Center">
                    <TextBlock Text="Ambiente" Style="{StaticResource ManagerMutedStyle}" />
                    <TextBlock Text="{Binding EnvironmentName}" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="4" VerticalAlignment="Center">
                    <TextBlock Text="Máquina" Style="{StaticResource ManagerMutedStyle}" />
                    <TextBlock Text="{Binding MachineName}" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="5" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Atualizar"
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            MinWidth="116"
                            Padding="14,9"
                            Margin="0,0,10,0" />
                    <Button Content="Abrir Web"
                            Command="{Binding OpenWebCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            MinWidth="100"
                            Padding="14,9" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="0.9*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource ManagerCardStyle}">
                <StackPanel>
                    <TextBlock Text="Recursos do Servidor" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,14" />
                    <UniformGrid Columns="3">
                        <StackPanel HorizontalAlignment="Center">
                            <Border Width="82" Height="82" CornerRadius="41" BorderBrush="#3B82F6" BorderThickness="9">
                                <TextBlock Text="{Binding CpuUsage}" Foreground="White" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <TextBlock Text="CPU" Style="{StaticResource ManagerMutedStyle}" HorizontalAlignment="Center" Margin="0,8,0,0" />
                        </StackPanel>
                        <StackPanel HorizontalAlignment="Center">
                            <Border Width="82" Height="82" CornerRadius="41" BorderBrush="#72D44E" BorderThickness="9">
                                <TextBlock Text="{Binding MemoryUsage}" Foreground="White" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <TextBlock Text="Memória" Style="{StaticResource ManagerMutedStyle}" HorizontalAlignment="Center" Margin="0,8,0,0" />
                        </StackPanel>
                        <StackPanel HorizontalAlignment="Center">
                            <Border Width="82" Height="82" CornerRadius="41" BorderBrush="#0EA5E9" BorderThickness="9">
                                <TextBlock Text="{Binding DiskUsage}" Foreground="White" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <TextBlock Text="Disco" Style="{StaticResource ManagerMutedStyle}" HorizontalAlignment="Center" Margin="0,8,0,0" />
                        </StackPanel>
                    </UniformGrid>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource ManagerCardStyle}">
                <StackPanel>
                    <TextBlock Text="Runtime" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,12" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30" />
                            <RowDefinition Height="30" />
                            <RowDefinition Height="30" />
                            <RowDefinition Height="30" />
                            <RowDefinition Height="30" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="Total de TAGs" Style="{StaticResource ManagerMutedStyle}" />
                        <TextBlock Grid.Column="1" Text="{Binding TotalTags}" Foreground="White" />
                        <TextBlock Grid.Row="1" Text="TAGs ativas" Style="{StaticResource ManagerMutedStyle}" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ActiveTags}" Foreground="#72D44E" />
                        <TextBlock Grid.Row="2" Text="Sem atualização" Style="{StaticResource ManagerMutedStyle}" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding StaleTags}" Foreground="#FACC15" />
                        <TextBlock Grid.Row="3" Text="Eventos na fila" Style="{StaticResource ManagerMutedStyle}" />
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding QueuedEvents}" Foreground="White" />
                        <TextBlock Grid.Row="4" Text="Último processamento" Style="{StaticResource ManagerMutedStyle}" />
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding LastProcessingAt}" Foreground="White" />
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource ManagerCardStyle}">
                <StackPanel>
                    <TextBlock Text="Conexões" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,12" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="31" />
                            <RowDefinition Height="31" />
                            <RowDefinition Height="31" />
                            <RowDefinition Height="31" />
                            <RowDefinition Height="31" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="MQTT Broker" Style="{StaticResource ManagerMutedStyle}" />
                        <TextBlock Grid.Column="1" Text="{Binding MqttStatus}" Foreground="#72D44E" />
                        <TextBlock Grid.Row="1" Text="OPC UA" Style="{StaticResource ManagerMutedStyle}" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding OpcStatus}" Foreground="#72D44E" />
                        <TextBlock Grid.Row="2" Text="Banco de Dados" Style="{StaticResource ManagerMutedStyle}" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding DatabaseStatus}" Foreground="#72D44E" />
                        <TextBlock Grid.Row="3" Text="API" Style="{StaticResource ManagerMutedStyle}" />
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding ApiStatus}" Foreground="#38BDF8" />
                        <TextBlock Grid.Row="4" Text="WebSocket" Style="{StaticResource ManagerMutedStyle}" />
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding WebSocketStatus}" Foreground="#38BDF8" />
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource ManagerCardStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Mensagens MQTT" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,12" />
                    <TextBlock Text="{Binding MqttReceived, StringFormat=Recebidas: {0}}" Style="{StaticResource ManagerMutedStyle}" Margin="0,0,0,8" />
                    <TextBlock Text="{Binding MqttSent, StringFormat=Enviadas: {0}}" Style="{StaticResource ManagerMutedStyle}" Margin="0,0,0,8" />
                    <TextBlock Text="{Binding MqttRetained, StringFormat=Retidas: {0}}" Style="{StaticResource ManagerMutedStyle}" Margin="0,0,0,8" />
                    <TextBlock Text="{Binding MqttTopics, StringFormat=Tópicos: {0}}" Style="{StaticResource ManagerMutedStyle}" Margin="0,0,0,8" />
                    <TextBlock Text="{Binding MqttClients, StringFormat=Clientes: {0}}" Style="{StaticResource ManagerMutedStyle}" />
                </StackPanel>
            </Border>
        </Grid>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="320" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Border Style="{StaticResource ManagerCardStyle}">
                    <StackPanel>
                        <TextBlock Text="TAGs - Últimas Atualizações" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,10" />
                        <DataGrid ItemsSource="{Binding TagRows}" Style="{StaticResource AdminDataGridStyle}" MinHeight="220">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Nome da TAG" Binding="{Binding Name}" Width="1.35*" />
                                <DataGridTextColumn Header="Máquina" Binding="{Binding Machine}" Width="*" />
                                <DataGridTextColumn Header="Driver" Binding="{Binding Driver}" Width="0.8*" />
                                <DataGridTextColumn Header="Endereço" Binding="{Binding Address}" Width="1.4*" />
                                <DataGridTextColumn Header="Valor" Binding="{Binding Value}" Width="0.7*" />
                                <DataGridTextColumn Header="Qualidade" Binding="{Binding Quality}" Width="0.8*" />
                                <DataGridTextColumn Header="Atualização" Binding="{Binding UpdatedAt}" Width="1.1*" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Style="{StaticResource ManagerCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Serviços" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,10" />
                            <DataGrid ItemsSource="{Binding ServiceRows}" Style="{StaticResource AdminDataGridStyle}" MinHeight="180">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Serviço" Binding="{Binding Name}" Width="1.2*" />
                                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" />
                                    <DataGridTextColumn Header="Uptime" Binding="{Binding Uptime}" Width="*" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="1" Style="{StaticResource ManagerCardStyle}" Margin="0,0,12,12">
                        <StackPanel>
                            <TextBlock Text="Logs - Últimos" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,10" />
                            <DataGrid ItemsSource="{Binding LogRows}" Style="{StaticResource AdminDataGridStyle}" MinHeight="180">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Data/Hora" Binding="{Binding DateTime}" Width="1.1*" />
                                    <DataGridTextColumn Header="Nível" Binding="{Binding Level}" Width="0.5*" />
                                    <DataGridTextColumn Header="Mensagem" Binding="{Binding Message}" Width="1.8*" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <Border Style="{StaticResource ManagerCardStyle}" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Banco de Dados" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,12" />
                        <TextBlock Text="{Binding DatabaseFile, StringFormat=Arquivo: {0}}" Style="{StaticResource ManagerMutedStyle}" Margin="0,0,0,7" />
                        <TextBlock Text="{Binding DatabaseSize, StringFormat=Tamanho: {0}}" Style="{StaticResource ManagerMutedStyle}" Margin="0,0,0,7" />
                        <TextBlock Text="{Binding DatabaseTables, StringFormat=Tabelas: {0}}" Style="{StaticResource ManagerMutedStyle}" Margin="0,0,0,7" />
                        <TextBlock Text="{Binding DatabaseRecords, StringFormat=Registros: {0}}" Style="{StaticResource ManagerMutedStyle}" Margin="0,0,0,14" />
                        <Button Content="Abrir logs"
                                Command="{Binding OpenLogsCommand}"
                                Style="{StaticResource SecondaryButtonStyle}"
                                HorizontalAlignment="Stretch" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource ManagerCardStyle}" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Ações Rápidas" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,12" />
                        <UniformGrid Columns="2">
                            <Button Content="Reiniciar Serviços"
                                    Command="{Binding RestartServicesCommand}"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    MinWidth="0"
                                    Margin="0,0,8,8" />
                            <Button Content="Sincronizar TAGs"
                                    Command="{Binding SyncTagsCommand}"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    MinWidth="0"
                                    Margin="0,0,0,8" />
                            <Button Content="Abrir Web"
                                    Command="{Binding OpenWebCommand}"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    MinWidth="0"
                                    Margin="0,0,8,0" />
                            <Button Content="Atualizar"
                                    Command="{Binding RefreshCommand}"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    MinWidth="0"
                                    Margin="0" />
                        </UniformGrid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource ManagerCardStyle}" Margin="0">
                    <StackPanel>
                        <TextBlock Text="Última leitura" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,12" />
                        <TextBlock Text="{Binding LastCheckedAt}" Foreground="#F8FAFC" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding DataDirectory}" Style="{StaticResource ManagerMutedStyle}" TextWrapping="Wrap" Margin="0,10,0,0" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>

<UserControl x:Class="AnalictY.Manager.Views.EventsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="ManagerPanelBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#17222C" Offset="0" />
            <GradientStop Color="#101820" Offset="1" />
        </LinearGradientBrush>

        <Style x:Key="ManagerCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource ManagerPanelBrush}" />
            <Setter Property="BorderBrush" Value="#2A3848" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="0,0,12,12" />
        </Style>

        <Style x:Key="ManagerTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="ManagerMutedStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AAB7C5" />
            <Setter Property="FontSize" Value="12" />
        </Style>

        <Style x:Key="MetricValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="AdminDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#E5EDF5" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#263445" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="RowHeaderWidth" Value="0" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="IsReadOnly" Value="True" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#DC2626" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#DC2626" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,9" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B91C1C" />
                    <Setter Property="BorderBrush" Value="#B91C1C" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#14202A" />
            <Setter Property="Foreground" Value="#D8E2EC" />
            <Setter Property="BorderBrush" Value="#263445" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#E5EDF5" />
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,6" />
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <Border Style="{StaticResource ManagerCardStyle}" Margin="0,0,0,12" Padding="22,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Eventos Administrativos" Style="{StaticResource ManagerTitleStyle}" />
                    <TextBlock Text="Linha do tempo de eventos e alertas do AnalictY Server" Style="{StaticResource ManagerMutedStyle}" Margin="0,6,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Reconhecer Selecionados" Command="{Binding AcknowledgeCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource ManagerCardStyle}">
                <StackPanel>
                    <TextBlock Text="Eventos Hoje" Style="{StaticResource ManagerMutedStyle}" />
                    <TextBlock Text="{Binding EventsToday}" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource ManagerCardStyle}">
                <StackPanel>
                    <TextBlock Text="Avisos" Style="{StaticResource ManagerMutedStyle}" />
                    <TextBlock Text="{Binding Warnings}" Foreground="#FACC15" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource ManagerCardStyle}">
                <StackPanel>
                    <TextBlock Text="Erros" Style="{StaticResource ManagerMutedStyle}" />
                    <TextBlock Text="{Binding Errors}" Foreground="#EF4444" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource ManagerCardStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Reconhecidos" Style="{StaticResource ManagerMutedStyle}" />
                    <TextBlock Text="{Binding Acknowledged}" Foreground="#72D44E" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Style="{StaticResource ManagerCardStyle}" Margin="0">
            <StackPanel>
                <TextBlock Text="Linha do Tempo de Eventos" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,10" />
                <DataGrid ItemsSource="{Binding EventRows}" Style="{StaticResource AdminDataGridStyle}" MinHeight="350">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Data/Hora" Binding="{Binding DateTime}" Width="1.2*" />
                        <DataGridTextColumn Header="Severidade" Binding="{Binding Severity}" Width="0.8*" />
                        <DataGridTextColumn Header="Origem" Binding="{Binding Source}" Width="*" />
                        <DataGridTextColumn Header="Mensagem" Binding="{Binding Message}" Width="2.5*" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>

<UserControl x:Class="AnalictY.Manager.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="ManagerPanelBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#30465F" Offset="0" />
            <GradientStop Color="#243447" Offset="1" />
        </LinearGradientBrush>

        <Style x:Key="ManagerCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource ManagerPanelBrush}" />
            <Setter Property="BorderBrush" Value="#4A617B" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="0,0,12,12" />
        </Style>

        <Style x:Key="ManagerTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="ManagerMutedStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AAB7C5" />
            <Setter Property="FontSize" Value="12" />
        </Style>

        <Style x:Key="AdminDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#E5EDF5" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#4A617B" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="RowHeaderWidth" Value="0" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="IsReadOnly" Value="True" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#DC2626" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#DC2626" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,9" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B91C1C" />
                    <Setter Property="BorderBrush" Value="#B91C1C" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#42617F" />
            <Setter Property="Foreground" Value="#E5EDF5" />
            <Setter Property="BorderBrush" Value="#334456" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,9" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A617B" />
                    <Setter Property="BorderBrush" Value="#42556A" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2A3D53" />
            <Setter Property="Foreground" Value="#D8E2EC" />
            <Setter Property="BorderBrush" Value="#4A617B" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#E5EDF5" />
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,6" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,0,20">
            <Border Style="{StaticResource ManagerCardStyle}" Margin="0,0,0,12" Padding="22,18">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Logs do Sistema" Style="{StaticResource ManagerTitleStyle}" />
                        <TextBlock Text="Visualizador de logs recentes do AnalictY Server" Style="{StaticResource ManagerMutedStyle}" Margin="0,6,0,0" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <ComboBox ItemsSource="{Binding LogLevels}" SelectedItem="{Binding SelectedLevel}" Width="120" Margin="0,0,8,0" />
                        <TextBox Text="{Binding SearchText}" Width="200" Margin="0,0,8,0" />
                        <Button Content="Carregar" Command="{Binding LoadCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0" />
                        <Button Content="Abrir Pasta" Command="{Binding OpenLogsFolderCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{StaticResource ManagerCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Total" Style="{StaticResource ManagerMutedStyle}" />
                        <TextBlock Text="{Binding TotalLogs}" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Style="{StaticResource ManagerCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Erros" Style="{StaticResource ManagerMutedStyle}" />
                        <TextBlock Text="{Binding ErrorLogs}" Foreground="#EF4444" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource ManagerCardStyle}" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Avisos" Style="{StaticResource ManagerMutedStyle}" />
                        <TextBlock Text="{Binding WarningLogs}" Foreground="#FACC15" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                    </StackPanel>
                </Border>
            </Grid>

            <Border Style="{StaticResource ManagerCardStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource ManagerMutedStyle}" Margin="0,0,0,8" />
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#EF4444" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource ManagerCardStyle}" Margin="0">
                <StackPanel>
                    <TextBlock Text="Logs Recentes" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,10" />
                    <DataGrid ItemsSource="{Binding LogEntries}" Style="{StaticResource AdminDataGridStyle}" MinHeight="350">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" Width="1.2*" />
                            <DataGridTextColumn Header="Nível" Binding="{Binding Level}" Width="0.6*" />
                            <DataGridTextColumn Header="Origem" Binding="{Binding Source}" Width="*" />
                            <DataGridTextColumn Header="Mensagem" Binding="{Binding Message}" Width="2.5*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>

<UserControl x:Class="AnalictY.Manager.Views.DashboardsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="760" d:DesignWidth="1280"
             Background="#F3F5F7"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <Style x:Key="WhiteCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="DangerButton" TargetType="Button">
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="ReadableTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="ReadableCombo" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">
                <Grid Margin="0,0,0,18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Width="40" Height="40" Margin="0,0,16,0" Background="Transparent" BorderThickness="0" Click="NavigateBackButton_Click">
                        <Path Data="M6 6L18 18M18 6L6 18" Stroke="#6B7280" StrokeThickness="2" Fill="Transparent" Width="20" Height="20" Stretch="Uniform"/>
                    </Button>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Dashboards" Foreground="#111827" FontSize="28" FontWeight="Bold"/>
                        <TextBlock Text="{Binding StatusMessage}" Foreground="#64748B" FontSize="14" Margin="0,4,0,0"/>
                    </StackPanel>
                    <Button Grid.Column="2" Content="Atualizar" Style="{StaticResource PrimaryButton}" Command="{Binding RefreshCommand}"/>
                </Grid>

                <Border x:Name="ErrorBorder" Background="#FEF2F2" BorderBrush="#FCA5A5" BorderThickness="1" CornerRadius="8"
                        Padding="14" Margin="0,0,0,14" Visibility="Collapsed">
                    <TextBlock x:Name="ErrorText" Foreground="#DC2626" FontWeight="SemiBold" TextWrapping="Wrap"/>
                </Border>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource WhiteCard}" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="{Binding FormTitle}" Foreground="#111827" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                            <TextBlock Text="Nome" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                            <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding DashboardName, Mode=TwoWay}" Margin="0,0,0,14"/>

                            <TextBlock Text="Máquina" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                            <ComboBox Style="{StaticResource ReadableCombo}" ItemsSource="{Binding Machines}" DisplayMemberPath="Name"
                                      SelectedItem="{Binding SelectedMachine, Mode=TwoWay}" Margin="0,0,0,14"/>

                            <TextBlock Text="Período" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                            <ComboBox Style="{StaticResource ReadableCombo}" SelectedValue="{Binding PeriodPreset, Mode=TwoWay}" SelectedValuePath="Tag" Margin="0,0,0,14">
                                <ComboBoxItem Content="Hoje" Tag="today"/>
                                <ComboBoxItem Content="Turno atual" Tag="shift"/>
                                <ComboBoxItem Content="Últimas 8h" Tag="last_8h"/>
                            </ComboBox>

                            <TextBlock Text="Atualização (segundos)" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                            <ComboBox Style="{StaticResource ReadableCombo}" SelectedValue="{Binding RefreshInterval, Mode=TwoWay}" SelectedValuePath="Tag" Margin="0,0,0,14">
                                <ComboBoxItem Content="5 segundos" Tag="5"/>
                                <ComboBoxItem Content="10 segundos" Tag="10"/>
                                <ComboBoxItem Content="30 segundos" Tag="30"/>
                                <ComboBoxItem Content="60 segundos" Tag="60"/>
                            </ComboBox>

                            <CheckBox Content="Dashboard padrão" IsChecked="{Binding IsDefault, Mode=TwoWay}" Margin="0,0,0,14"/>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <Button Content="Salvar" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}" Margin="0,0,8,0"/>
                                <Button Content="Limpar" Style="{StaticResource SecondaryButton}" Command="{Binding CreateCommand}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource WhiteCard}">
                        <StackPanel>
                            <TextBlock Text="Dashboards" Foreground="#111827" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                            <DataGrid ItemsSource="{Binding Dashboards}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                                      SelectedItem="{Binding SelectedDashboard}" Height="400">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="1.5*"/>
                                    <DataGridTextColumn Header="Máquina" Binding="{Binding MachineId}" Width="*"/>
                                    <DataGridTextColumn Header="Período" Binding="{Binding PeriodPreset}" Width="*"/>
                                    <DataGridTextColumn Header="Atualização" Binding="{Binding RefreshInterval}" Width="*"/>
                                    <DataGridTextColumn Header="Padrão" Binding="{Binding IsDefault, Converter={StaticResource BoolToYesNo}}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                                <Button Content="Editar" Style="{StaticResource SecondaryButton}" Command="{Binding EditCommand}" Margin="0,0,8,0"/>
                                <Button Content="Excluir" Style="{StaticResource DangerButton}" Command="{Binding DeleteCommand}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

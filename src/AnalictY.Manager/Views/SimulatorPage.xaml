<UserControl x:Class="AnalictY.Manager.Views.SimulatorPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="760" d:DesignWidth="1280"
             Background="#F3F5F7"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <Style x:Key="WhiteCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CyanButton" TargetType="Button">
            <Setter Property="Background" Value="#0891B2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ReadableTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">
                <!-- Header -->
                <Border Style="{StaticResource WhiteCard}" Margin="0,0,0,18">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Button Width="40" Height="40" Margin="0,0,12,0" Background="Transparent" BorderThickness="0" Click="NavigateBackButton_Click">
                            <Path Data="M10 19l-7-7m0 0l7-7m-7 7h18" Stroke="#6B7280" StrokeThickness="2" Fill="Transparent" Width="20" Height="20" Stretch="Uniform"/>
                        </Button>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Simulador MES" Foreground="#111827" FontSize="24" FontWeight="Bold"/>
                            <TextBlock Text="Crie e gerencie máquinas virtuais para testes" Foreground="#6B7280" FontSize="14" Margin="0,4,0,0"/>
                        </StackPanel>
                        <Button Grid.Column="2" Content="Atualizar" Style="{StaticResource PrimaryButton}" Command="{Binding RefreshCommand}" Margin="12,0,0,0"/>
                    </Grid>
                </Border>

                <Border x:Name="ErrorBorder" Background="#FEF2F2" BorderBrush="#FCA5A5" BorderThickness="1" CornerRadius="8"
                        Padding="14" Margin="0,0,0,14" Visibility="Collapsed">
                    <TextBlock x:Name="ErrorText" Foreground="#DC2626" FontWeight="SemiBold" TextWrapping="Wrap"/>
                </Border>

                <Grid Visibility="{Binding HasConsole, Converter={StaticResource InverseBoolToVisibility}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource WhiteCard}" Margin="0,0,6,0">
                        <StackPanel>
                            <TextBlock Text="Criar máquina virtual" Foreground="#111827" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>
                            <TextBlock Text="Cria máquina, TAGs e vínculos MES automaticamente." Foreground="#6B7280" FontSize="13" Margin="0,0,0,16"/>
                            
                            <Border Background="#F8FAFC" CornerRadius="8" Padding="16">
                                <StackPanel>
                                    <TextBlock Text="Nome *" Style="{StaticResource LabelText}"/>
                                    <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding MachineName, Mode=TwoWay}" Margin="0,0,0,12"/>
                                    <TextBlock Text="Código *" Style="{StaticResource LabelText}"/>
                                    <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding MachineCode, Mode=TwoWay}" Margin="0,0,0,12"/>
                                    <TextBlock Text="Centro de custo *" Style="{StaticResource LabelText}"/>
                                    <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding CostCenter, Mode=TwoWay}" Margin="0,0,0,12"/>
                                    <TextBlock Text="Local" Style="{StaticResource LabelText}"/>
                                    <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding Location, Mode=TwoWay}" Margin="0,0,0,12"/>
                                    <Button Content="Criar bancada" Style="{StaticResource CyanButton}" Command="{Binding CreateMachineCommand}" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource WhiteCard}" Margin="6,0,0,0">
                        <StackPanel>
                            <TextBlock Text="Bancadas existentes" Foreground="#111827" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>
                            <ListBox ItemsSource="{Binding Machines}" SelectedValue="{Binding SelectedMachineId, Mode=TwoWay}" SelectedValuePath="Id" Height="360" BorderThickness="0">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Button Command="{Binding DataContext.OpenMachineCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Background="Transparent" BorderThickness="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Name}" Foreground="#111827" FontWeight="SemiBold"/>
                                                    <TextBlock Text="{Binding Code}" Foreground="#6B7280" FontSize="12"/>
                                                </StackPanel>
                                                <TextBlock Grid.Column="1" Text="Abrir" Foreground="#0891B2" FontSize="12" Margin="8,0,0,0"/>
                                            </Grid>
                                        </Button>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                    </Border>
                </Grid>

                <StackPanel Visibility="{Binding HasConsole, Converter={StaticResource BoolToVisibility}}">
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource WhiteCard}" Margin="0,0,6,0">
                            <StackPanel>
                                <TextBlock Text="Máquina" Foreground="#6B7280" FontSize="12" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Console.Machine.Name, FallbackValue='-'}" Foreground="#111827" FontSize="20" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Console.Machine.Code, FallbackValue='-'}" Foreground="#6B7280" FontSize="12"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Style="{StaticResource WhiteCard}" Margin="6,0">
                            <StackPanel>
                                <TextBlock Text="Produção" Foreground="#6B7280" FontSize="12" FontWeight="Bold"/>
                                <TextBlock Text="{Binding ProductionCounter}" Foreground="#111827" FontSize="20" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Style="{StaticResource WhiteCard}" Margin="6,0">
                            <StackPanel>
                                <TextBlock Text="Perdas" Foreground="#6B7280" FontSize="12" FontWeight="Bold"/>
                                <TextBlock Text="{Binding LossCounter}" Foreground="#111827" FontSize="20" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="3" Style="{StaticResource WhiteCard}" Margin="6,0,0,0">
                            <StackPanel>
                                <TextBlock Text="Status" Foreground="#6B7280" FontSize="12" FontWeight="Bold"/>
                                <TextBlock Text="{Binding StatusLabel}" Foreground="#111827" FontSize="20" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Reason, StringFormat='{}motivo {0}'}" Foreground="#6B7280" FontSize="12"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.25*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource WhiteCard}" Margin="0,0,6,0">
                            <StackPanel>
                                <TextBlock Text="Comando da máquina" Foreground="#111827" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>
                                <TextBlock Text="Status (0=Inativa, 1=Produção, 2=Ociosa, 3=Manutenção)" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                                <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding Status, Mode=TwoWay}" Margin="0,0,0,12"/>
                                <TextBlock Text="Motivo de parada" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                                <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding Reason, Mode=TwoWay}" Margin="0,0,0,12"/>
                                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                                    <Button Content="Produzir 1 peça" Style="{StaticResource PrimaryButton}" Command="{Binding ProduceOneCommand}" Margin="0,0,6,0"/>
                                    <Button Content="Registrar perda" Style="{StaticResource CyanButton}" Command="{Binding AddLossCommand}" Margin="0,0,6,0"/>
                                    <Button Content="Publicar estado" Style="{StaticResource SecondaryButton}" Command="{Binding PublishCommand}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Style="{StaticResource WhiteCard}" Margin="6,0,0,0">
                            <StackPanel>
                                <TextBlock Text="Produção automática" Foreground="#111827" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>
                                <TextBlock Text="Peças por minuto" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                                <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding PiecesPerMinute, Mode=TwoWay}" Margin="0,0,0,12"/>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Iniciar" Style="{StaticResource PrimaryButton}" Command="{Binding StartAutomaticCommand}" Margin="0,0,6,0"/>
                                    <Button Content="Parar" Style="{StaticResource SecondaryButton}" Command="{Binding StopAutomaticCommand}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource WhiteCard}" Margin="0,0,6,0">
                            <StackPanel>
                                <TextBlock Text="Motivos de parada" Foreground="#111827" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>
                                <ListBox ItemsSource="{Binding Console.Reasons}" Height="220" BorderThickness="0">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Description}" Foreground="#111827" Margin="0,0,0,6"/>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Style="{StaticResource WhiteCard}" Margin="6,0,0,0">
                            <StackPanel>
                                <TextBlock Text="TAGs virtuais" Foreground="#111827" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>
                                <ItemsControl ItemsSource="{Binding Console.Tags}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#F3F4F6" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,6">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Key}" Foreground="#111827" FontWeight="SemiBold"/>
                                                    <TextBlock Text="{Binding Value.Name}" Foreground="#6B7280" FontSize="13"/>
                                                    <TextBlock Text="{Binding Value.Address}" Foreground="#9CA3AF" FontSize="11"/>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Button Content="Voltar para bancadas" Style="{StaticResource SecondaryButton}" Command="{Binding BackToMachinesCommand}" HorizontalAlignment="Right" Margin="0,14,0,0"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

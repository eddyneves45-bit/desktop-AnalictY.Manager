<UserControl x:Class="AnalictY.Manager.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="ManagerPanelBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#30465F" Offset="0" />
            <GradientStop Color="#243447" Offset="1" />
        </LinearGradientBrush>

        <Style x:Key="ManagerCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource ManagerPanelBrush}" />
            <Setter Property="BorderBrush" Value="#4A617B" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="0,0,12,12" />
        </Style>

        <Style x:Key="ManagerTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="ManagerMutedStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AAB7C5" />
            <Setter Property="FontSize" Value="12" />
        </Style>

        <Style x:Key="MetricValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#42617F" />
            <Setter Property="Foreground" Value="#E5EDF5" />
            <Setter Property="BorderBrush" Value="#334456" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,9" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A617B" />
                    <Setter Property="BorderBrush" Value="#42556A" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <Border Style="{StaticResource ManagerCardStyle}" Margin="0,0,0,12" Padding="22,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="{Binding ProductName}" Style="{StaticResource ManagerTitleStyle}" />
                    <TextBlock Text="Informações de versão e sistema" Style="{StaticResource ManagerMutedStyle}" Margin="0,6,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Documentação" Command="{Binding OpenDocsCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" />
                    <Button Content="Suporte" Command="{Binding OpenSupportCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource ManagerCardStyle}">
                <StackPanel>
                    <TextBlock Text="Versão" Style="{StaticResource ManagerMutedStyle}" />
                    <TextBlock Text="{Binding Version}" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource ManagerCardStyle}">
                <StackPanel>
                    <TextBlock Text="Build" Style="{StaticResource ManagerMutedStyle}" />
                    <TextBlock Text="{Binding Build}" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource ManagerCardStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Autor" Style="{StaticResource ManagerMutedStyle}" />
                    <TextBlock Text="{Binding Author}" Style="{StaticResource MetricValueStyle}" Margin="0,5,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Border Style="{StaticResource ManagerCardStyle}" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Caminho de Instalação" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,10" />
                        <TextBlock Text="{Binding InstallPath}" Style="{StaticResource ManagerMutedStyle}" TextWrapping="Wrap" FontFamily="Consolas" FontSize="11" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource ManagerCardStyle}" Margin="0">
                    <StackPanel>
                        <TextBlock Text="Diretório de Dados" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,10" />
                        <TextBlock Text="{Binding DataDirectory}" Style="{StaticResource ManagerMutedStyle}" TextWrapping="Wrap" FontFamily="Consolas" FontSize="11" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <Border Style="{StaticResource ManagerCardStyle}" Margin="0">
                    <StackPanel>
                        <TextBlock Text="Sobre o AnalictY Server Manager" Style="{StaticResource ManagerTitleStyle}" Margin="0,0,0,10" />
                        <TextBlock Text="Console administrativo para gerenciamento do AnalictY Server. Permite monitoramento de runtime, configuração de drivers (MQTT, OPC UA), gerenciamento de tags, serviços, logs, eventos, backup e licenciamento." Style="{StaticResource ManagerMutedStyle}" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>

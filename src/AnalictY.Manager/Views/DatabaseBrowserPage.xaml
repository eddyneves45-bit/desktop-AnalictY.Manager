<UserControl x:Class="AnalictY.Manager.Views.DatabaseBrowserPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="760" d:DesignWidth="1280"
             Background="#F3F5F7"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <Style x:Key="WhiteCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="BlueButton" TargetType="Button">
            <Setter Property="Background" Value="#1D4ED8"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="ReadableCombo" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="ReadableTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">
                <Grid Margin="0,0,0,18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Width="40" Height="40" Margin="0,0,16,0" Background="Transparent" BorderThickness="0" Click="NavigateBackButton_Click">
                        <Path Data="M6 6L18 18M18 6L6 18" Stroke="#6B7280" StrokeThickness="2" Fill="Transparent" Width="20" Height="20" Stretch="Uniform"/>
                    </Button>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Browser de Banco" Foreground="#111827" FontSize="28" FontWeight="Bold"/>
                        <TextBlock Text="{Binding StatusMessage}" Foreground="#64748B" FontSize="14" Margin="0,4,0,0"/>
                    </StackPanel>
                    <Button Grid.Column="2" Content="Atualizar" Style="{StaticResource BlueButton}" Command="{Binding RefreshCommand}"/>
                </Grid>

                <Border x:Name="ErrorBorder" Background="#FEF2F2" BorderBrush="#FCA5A5" BorderThickness="1" CornerRadius="8"
                        Padding="14" Margin="0,0,0,14" Visibility="Collapsed">
                    <TextBlock x:Name="ErrorText" Foreground="#DC2626" FontWeight="SemiBold" TextWrapping="Wrap"/>
                </Border>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Connection -->
                    <Border Grid.Column="0" Style="{StaticResource WhiteCard}" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="Conexão" Foreground="#111827" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                            
                            <TextBlock Text="Banco configurado" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                            <ComboBox ItemsSource="{Binding Connections}" DisplayMemberPath="DisplayName" SelectedValuePath="Id" 
                                      SelectedValue="{Binding SelectedConnectionId}" Style="{StaticResource ReadableCombo}"/>
                        </StackPanel>
                    </Border>

                    <!-- Database -->
                    <Border Grid.Column="1" Style="{StaticResource WhiteCard}" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="Bancos" Foreground="#111827" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                            
                            <TextBlock Text="Database" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                            <ComboBox ItemsSource="{Binding Databases}" DisplayMemberPath="Name" SelectedValuePath="Name" 
                                      SelectedValue="{Binding SelectedDatabase}" Style="{StaticResource ReadableCombo}"/>
                        </StackPanel>
                    </Border>

                    <!-- Search -->
                    <Border Grid.Column="2" Style="{StaticResource WhiteCard}" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="Consulta" Foreground="#111827" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                            
                            <TextBlock Text="Filtro texto" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                            <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding Search, Mode=TwoWay}" Margin="0,0,0,14"/>
                            
                            <TextBlock Text="Data Início" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                            <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding DateFrom, Mode=TwoWay}" Margin="0,0,0,14"/>
                            
                            <TextBlock Text="Data Fim" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                            <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding DateTo, Mode=TwoWay}"/>
                        </StackPanel>
                    </Border>

                    <!-- Limit -->
                    <Border Grid.Column="3" Style="{StaticResource WhiteCard}">
                        <StackPanel>
                            <TextBlock Text="Paginação" Foreground="#111827" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                            
                            <TextBlock Text="Limite" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                            <ComboBox SelectedIndex="0" Style="{StaticResource ReadableCombo}" Margin="0,0,0,14">
                                <ComboBoxItem Content="50"/>
                                <ComboBoxItem Content="100" IsSelected="True"/>
                                <ComboBoxItem Content="250"/>
                                <ComboBoxItem Content="500"/>
                            </ComboBox>

                            <Button Content="Carregar Dados" Style="{StaticResource BlueButton}" Command="{Binding LoadDataCommand}" Margin="0,8,0,0"/>
                            <Button Content="Exportar CSV" Style="{StaticResource SecondaryButton}" Click="ExportCsv_Click" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Tables -->
                <Border Style="{StaticResource WhiteCard}" Margin="0,18,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="300"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Tables List -->
                        <Border Grid.Column="0" BorderBrush="#E5E7EB" BorderThickness="0,0,1,0" Padding="0,0,16,0">
                            <StackPanel>
                                <TextBlock Text="Tabelas" Foreground="#111827" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                                <ItemsControl ItemsSource="{Binding Tables}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button Background="Transparent" BorderThickness="0" Padding="10,8" 
                                                    HorizontalContentAlignment="Left" Margin="0,0,0,4"
                                                    Click="TableButton_Click"
                                                    Tag="{Binding}">
                                                <TextBlock Text="{Binding Name}" Foreground="#111827" FontSize="13"/>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <!-- Columns and Data -->
                        <Grid Grid.Column="1" Margin="16,0,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Columns -->
                            <Border Grid.Row="0" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,14">
                                <TextBlock Text="Colunas" Foreground="#111827" FontSize="16" FontWeight="SemiBold"/>
                            </Border>
                            <DataGrid Grid.Row="1" ItemsSource="{Binding Columns}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" 
                                      Height="120" Margin="0,0,0,14">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="1.5*"/>
                                    <DataGridTextColumn Header="Tipo" Binding="{Binding DataType}" Width="*"/>
                                    <DataGridTextColumn Header="Nulo" Binding="{Binding Nullable, Converter={StaticResource BoolToYesNo}}" Width="*"/>
                                    <DataGridTextColumn Header="Chave" Binding="{Binding Key}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>

                            <!-- Rows -->
                            <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="4" Padding="12">
                                <TextBlock Text="Preview de dados" Foreground="#111827" FontSize="16" FontWeight="SemiBold"/>
                            </Border>
                            <DataGrid Grid.Row="2" ItemsSource="{Binding Rows}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" 
                                      Margin="0,40,0,0" VerticalAlignment="Stretch">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Dados" Binding="{Binding}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Pagination -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                    <Button Content="Anterior" Style="{StaticResource SecondaryButton}" Command="{Binding PreviousPageCommand}" IsEnabled="{Binding CanGoPrevious}"/>
                    <TextBlock Text="{Binding Offset, StringFormat='Offset: {0}'}" VerticalAlignment="Center" Margin="12,0"/>
                    <Button Content="Próxima" Style="{StaticResource SecondaryButton}" Command="{Binding NextPageCommand}" IsEnabled="{Binding CanGoNext}"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

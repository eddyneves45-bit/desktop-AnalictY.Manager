<UserControl x:Class="AnalictY.Manager.Views.UsersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="760" d:DesignWidth="1280"
             Background="#F3F5F7"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <Style x:Key="WhiteCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button">
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="ReadableTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">
                <Grid Margin="0,0,0,18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Width="40" Height="40" Margin="0,0,16,0" Background="Transparent" BorderThickness="0" Click="NavigateBackButton_Click">
                        <Path Data="M6 6L18 18M18 6L6 18" Stroke="#6B7280" StrokeThickness="2" Fill="Transparent" Width="20" Height="20" Stretch="Uniform"/>
                    </Button>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Usuários e Políticas" Foreground="#111827" FontSize="28" FontWeight="Bold"/>
                        <TextBlock Text="Admin tem acesso total. Usuário visualiza. Personalizado recebe permissões marcadas." Foreground="#64748B" FontSize="14" Margin="0,4,0,0"/>
                    </StackPanel>
                    <Button Grid.Column="2" Content="Atualizar" Style="{StaticResource PrimaryButton}" Command="{Binding RefreshCommand}"/>
                </Grid>

                <Border x:Name="ErrorBorder" Background="#FEF2F2" BorderBrush="#FCA5A5" BorderThickness="1" CornerRadius="8"
                        Padding="14" Margin="0,0,0,14" Visibility="Collapsed">
                    <TextBlock x:Name="ErrorText" Foreground="#DC2626" FontWeight="SemiBold" TextWrapping="Wrap"/>
                </Border>

                <Border Style="{StaticResource WhiteCard}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <TextBlock Text="Formulário de Usuário" Foreground="#111827" FontSize="18" FontWeight="SemiBold"/>
                            <Button Content="Novo Usuário" Style="{StaticResource PrimaryButton}" Command="{Binding CreateUserCommand}" Margin="8,0,0,0"/>
                        </StackPanel>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <TextBlock Text="Usuário" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                                <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding Username, Mode=TwoWay}" IsEnabled="{Binding IsEditingUser, Converter={StaticResource InverseBoolConverter}}" Margin="0,0,0,14"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" Margin="0,0,8,0">
                                <TextBlock Text="Email" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                                <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding Email, Mode=TwoWay}" IsEnabled="{Binding IsEditingUser, Converter={StaticResource InverseBoolConverter}}" Margin="0,0,0,14"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="2" Margin="0,0,8,0">
                                <TextBlock Text="Senha" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                                <TextBox Style="{StaticResource ReadableTextBox}" Text="{Binding Password, Mode=TwoWay}" Margin="0,0,0,14"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="3" Margin="0,0,8,0">
                                <TextBlock Text="Perfil" Foreground="#1F2937" FontSize="13" Margin="0,0,0,6"/>
                                <ComboBox SelectedValuePath="Content" SelectedValue="{Binding Role, Mode=TwoWay}" IsEnabled="{Binding IsEditingUser, Converter={StaticResource InverseBoolConverter}}" Margin="0,0,0,14">
                                    <ComboBoxItem Content="user"/>
                                    <ComboBoxItem Content="custom"/>
                                    <ComboBoxItem Content="admin"/>
                                </ComboBox>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="4">
                                <Button Content="Salvar" Style="{StaticResource PrimaryButton}" Command="{Binding SaveUserCommand}" Margin="0,0,0,14"/>
                            </StackPanel>
                        </Grid>
                        
                        <Border Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,14" Visibility="{Binding ShowPermissions, Converter={StaticResource BoolToVisibilityConverter}}">
                            <StackPanel>
                                <TextBlock Text="Permissões Personalizadas" Foreground="#111827" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <CheckBox Content="Mudar metas" IsChecked="{Binding PermGoalsManage, Mode=TwoWay}" Margin="0,0,0,4"/>
                                <CheckBox Content="Baixar relatórios" IsChecked="{Binding PermReportsDownload, Mode=TwoWay}" Margin="0,0,0,4"/>
                                <CheckBox Content="Criar alertas" IsChecked="{Binding PermAlertRulesManage, Mode=TwoWay}" Margin="0,0,0,4"/>
                                <CheckBox Content="Criar usuários" IsChecked="{Binding PermUsersManage, Mode=TwoWay}" Margin="0,0,0,4"/>
                                <CheckBox Content="Acessar auditoria" IsChecked="{Binding PermAuditView, Mode=TwoWay}"/>
                            </StackPanel>
                        </Border>
                        
                        <Separator Margin="0,14,0,14"/>
                        
                        <DataGrid ItemsSource="{Binding Users}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                                  SelectedItem="{Binding SelectedUser}" Height="300">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*"/>
                                <DataGridTextColumn Header="Usuário" Binding="{Binding Username}" Width="1.5*"/>
                                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*"/>
                                <DataGridTextColumn Header="Perfil" Binding="{Binding Role}" Width="*"/>
                                <DataGridTextColumn Header="Ativo" Binding="{Binding IsActive, Converter={StaticResource BoolToYesNo}}" Width="*"/>
                                <DataGridTextColumn Header="MFA" Binding="{Binding MfaEnabled, Converter={StaticResource BoolToYesNo}}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                            <Button Content="Editar" Style="{StaticResource SecondaryButton}" Command="{Binding EditUserCommand}" Margin="0,0,8,0"/>
                            <Button Content="Excluir" Style="{StaticResource DangerButton}" Command="{Binding DeleteUserCommand}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
